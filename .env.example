# Copy this file to `.env.local` for local development.
# GitHub Pages builds read the same keys from GitHub Actions secrets / variables.
VITE_GEMINI_API_KEY=your_gemini_api_key
VITE_GEMINI_MODEL=gemini-2.5-flash
